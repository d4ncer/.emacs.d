;;; rk-mermaid.el --- Mermaidy things  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(eval-when-compile
  (require 'use-package))

(use-package mermaid-mode
  :straight t)

(provide 'rk-mermaid)

;;; rk-mermaid.el ends here
