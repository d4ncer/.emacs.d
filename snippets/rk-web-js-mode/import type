# -*- mode: snippet -*-
# name: import type
# key: it
# condition: (and (rk-yas/bol?) (not (save-excursion (search-backward-regexp (rx bol (* space) symbol-start "describe" symbol-end) nil t))))
# --
import type { ${1:T} } from '$0';